[phases.build]
cmds = [
  "npm install",
  "npx prisma generate",
  "npx prisma db push --accept-data-loss",
  "npm run build"
]

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[start]
cmd = "npm start"
